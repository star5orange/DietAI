# DietAI — 真实宠物健康管理 需求文档（探索方向）

> **版本**: v1.0  
> **更新日期**: 2026-07-16   
> **文档目的**: 在 DietAI「人的健康管理」基础上，探索延伸至「真实宠物健康管理」方向，明确功能范围、技术可行性、复用方案与工作量评估

---

## 一、方向概述

### 1.1 定位

DietAI 现有核心能力围绕 **人的饮食健康管理**（拍照识别、营养分析、AI 建议、提醒养成）和 **虚拟宠物养成**（M2 已实现）。

本方向探索新增**可选的**「真实宠物健康管理」模块——用户可录入家中真实宠物（猫/狗等），通过智能硬件（自动喂食器/饮水机）联动，**定时放粮放水自动生成饮食记录**，结合 AI 分析宠物营养摄入、体重变化、健康提醒。

### 1.2 与虚拟桌宠的关系（重要）

**真实宠物和虚拟桌宠是两套完全独立的系统，互不替代、互不干扰。**

```
用户打开 App → 「我的宠物」页面
                         │
          ┌──────────────┴──────────────┐
          ▼                              ▼
   【精灵伙伴 Tab】                【真实宠物 Tab】
   (始终存在，默认显示)             (用户添加真实宠物后才有)
          │                              │
    虚拟精灵养成                    真实宠物健康管理
    - mood/level/exp               - 品种/体重/疫苗
    - 与人的饮食达标率联动           - 硬件喂食器自动记录
    - 皮肤/动作/成就解锁            - 营养分析/健康建议
    - 硬件时钟展示精灵动画           - 硬件时钟可切换展示
```

**三种用户场景：**

| 场景 | 用户行为 | 虚拟桌宠 | 真实宠物模块 |
|------|---------|---------|------------|
| **A：只用桌宠**（大多数用户） | 不添加任何真实宠物 | **正常工作**，根据人的饮食达标率计算 mood/exp | 不显示，零影响 |
| **B：只管理真实宠物** | 添加了真实宠物，不关心桌宠 | 可通过现有开关隐藏 | 正常工作 |
| **C：两者都用** | 既有虚拟精灵，又管理真实宠物 | **正常工作**，不受真实宠物数据影响 | 正常工作 |

> **核心原则：虚拟桌宠的 mood/level/exp 永远只和「人」的健康数据绑定。添加了真实宠物后，虚拟精灵的行为丝毫不受影响——它不会变成"真实宠物的镜像"，也不会读取真实宠物的数据。**

本方向不是"升级"虚拟桌宠，而是在它**旁边**新增一个独立的可选模块。

### 1.3 核心差异：从虚拟宠物到真实宠物

| 维度 | M2 虚拟宠物（保留不变） | 本方向：真实宠物（新增可选） |
|------|----------------------|--------------------------|
| 启用条件 | 所有用户默认可用 | 用户主动添加真实宠物后才启用 |
| 数据来源 | 用户饮食达标率 → mood 计算 | 硬件投粮/放水 → 真实饮食记录 |
| 交互方式 | App 内点击互动（抚摸/喂食/玩耍） | 硬件自动喂食器定时触发 |
| 健康指标 | mood（正常/开心/饥饿/焦虑/虚弱） | 真实体重、热量摄入、蛋白质摄入 |
| 提醒内容 | "该喝水了"（针对人） | "该驱虫了"、"该打疫苗了"（针对宠物） |
| 数据持久性 | 游戏化，重新开始也无妨 | 真实健康数据，需长期保留 |

### 1.3 与现有系统的关系

```
┌─────────────────────────────────────────────────────────────────┐
│                     DietAI 现有系统                               │
│                                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │
│  │ 人的饮食  │  │ 人的运动  │  │ AI 分析  │  │ 硬件对接      │    │
│  │ 记录管理  │  │ 饮水管理  │  │ Agent    │  │ (已就绪)      │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────┬───────┘    │
│                                                    │             │
│  ┌─────────────────────────────────────────────────┘             │
│  │  本方向新增                                                  │
│  ▼                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │
│  │ 真实宠物  │  │ 宠物饮食  │  │ 宠物AI   │  │ 硬件喂食器    │    │
│  │ 档案管理  │  │ 自动记录  │  │ 健康顾问 │  │ 饮水机联动   │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘    │
│                                                                  │
│  复用率: 数据库模型 ~60% / 服务层 ~50% / AI Agent ~40%           │
│         硬件对接层 ~80% / 前端组件 ~40%                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、功能规划

> 按实现优先级分三层，每层可独立交付，不强制全部完成。

---

### 功能 1：宠物档案管理（基础层 / P0）

#### 1.1 需求背景

用户需要能录入家中真实宠物的基础信息。M2 的 `virtual_pet_states` 表服务于虚拟精灵养成（mood/level/exp/skin），与真实宠物（品种/年龄/体重/疫苗）概念不同，需要新建数据模型。

#### 1.2 新增功能需求

##### 1.2.1 宠物基础档案

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 宠物信息录入 | 支持添加多只宠物（一猫一狗等），字段：宠物名、品种、性别、出生日期、是否绝育、头像 | 前端：新建 `pet_profile_page.dart`；后端：新建 `pet_profiles` 表 | P0 |
| 品种选择器 | 提供常见猫/狗品种列表，支持搜索；同时支持「混血」（已知是串串，如英短混布偶）和「其他」（未知品种，如流浪猫/冷门品种）手动输入 | 前端：品种选择组件 | P0 |
| 宠物列表 | 在"我的宠物"页展示所有宠物卡片，点击进入详情 | 前端：改造 `my_pet_page.dart`，增加真实宠物 Tab；后端：GET /api/pets | P0 |
| 宠物编辑/删除 | 支持修改宠物信息和删除（需二次确认） | 前端/后端 | P0 |

##### 1.2.2 宠物体重追踪

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 体重记录 | 记录宠物每次称重数据（体重 + 日期），自动计算 BMI（宠物专用公式） | 前端：体重记录组件；后端：新建 `pet_weight_records` 表 | P0 |
| 体重趋势图 | 展示宠物体重变化曲线，标注理想体重范围（按品种自动推荐） | 前端：体重趋势图（复用 fl_chart）；后端：GET /api/pets/{id}/weight-trend | P0 |
| 超重/偏瘦提醒 | 当体重偏离品种标准范围 >15% 时给出提醒 | 后端：计算 + 前端展示 | P1 |

##### 1.2.3 疫苗与驱虫记录

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 疫苗记录 | 记录疫苗名称、接种日期、有效期、下次接种日期 | 后端：新建 `pet_vaccine_records` 表 | P0 |
| 驱虫记录 | 记录驱虫类型（体内/体外）、日期、下次驱虫日期 | 后端：新建 `pet_deworming_records` 表 | P1 |
| 到期提醒 | 疫苗/驱虫到期前 3 天推送 APP 通知 | 后端：定时任务检查；前端：复用 `reminder` 模块 | P1 |

#### 1.3 验收标准

- [ ] 用户可添加/编辑/删除真实宠物档案
- [ ] 支持录入宠物品种、年龄、性别、绝育状态
- [ ] 宠物体重记录可增删改查，趋势图正常展示
- [ ] 疫苗/驱虫记录可增删改查
- [ ] 疫苗到期提醒正常触发

---

### 功能 2：硬件联动自动饮食记录（核心亮点层 / P0）

#### 2.1 需求背景

用户手动记录宠物每餐饮食体验差（没人愿意每天给猫称粮然后打开 App 记录）。通过智能硬件（自动喂食器 + 饮水机）定时投粮/放水时自动生成饮食记录，实现"零手动录入"。

> **关键复用**：现有 `hardware_router.py` 和 `hardware_service.py` 已实现硬件快捷按钮和离线同步逻辑，本功能在现有硬件对接层上扩展 `target_type=pet` 分支。

#### 2.2 新增功能需求

##### 2.2.1 硬件协议扩展

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 目标类型字段 | 硬件同步接口 `/api/hardware/sync` 请求体增加 `target_type` 字段（`human` / `pet`），区分人与宠物 | 后端：`hardware_router.py` 修改 | P0 |
| 宠物喂食记录协议 | 硬件投粮后发送：`{"target_type": "pet", "pet_id": 1, "type": "feeding", "food_name": "皇家猫粮", "amount_grams": 50, "calories": 210, "protein": 17.5, "timestamp": "..."}` | 后端：`hardware_service.py` 新增 `_sync_pet_feeding()` | P0 |
| 宠物饮水记录协议 | 硬件放水后发送：`{"target_type": "pet", "pet_id": 1, "type": "water", "amount_ml": 150, "timestamp": "..."}` | 后端：同上 | P0 |
| 喂食计划配置 | 硬件可查询当日喂食计划（次数、每份克数、时间），自动执行 | 后端：GET /api/hardware/pet-feeding-plan/{pet_id} | P1 |

##### 2.2.2 宠物饮食记录数据模型

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 宠物饮食记录表 | 新建 `pet_feeding_records` 表：pet_id, food_name, amount_grams, calories, protein, fat, carbs, record_time, from_source(hardware/manual) | 后端：新建 `pet_feeding_models.py` | P0 |
| 宠物饮水记录表 | 新建 `pet_water_records` 表：pet_id, amount_ml, record_time, from_source | 后端：同上 | P0 |
| 宠物每日营养汇总 | 新建 `pet_daily_summaries` 表：pet_id, summary_date, total_calories, total_protein, total_fat, total_carbs, total_water_ml, meal_count | 后端：同上 | P0 |

> **为什么不复用人的 `food_records` 表**：人的表有 `meal_type`(早/午/晚)、`recording_method`(拍照/手动)、`cost`(消费金额) 等人类特有字段，强行复用会导致大量 NULL 字段。新建宠物专用表结构更清晰，且后续可独立优化索引。

##### 2.2.3 手动补录（硬件离线时兜底）

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 手动记录宠物饮食 | 当硬件离线/未连接时，用户可手动录入宠物吃了什么 | 前端：宠物详情页 + 快速记录入口；后端：POST /api/pets/{id}/feeding-records | P1 |
| 宠物食品库 | 预置常见宠物食品营养数据（猫粮/狗粮/零食品牌），支持从库中选择快速记录 | 后端：新建 `pet_food_database` 表 + 初始化脚本 | P1 |

##### 2.2.4 复用：虚拟宠物状态联动

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 喂食后动画触发 | 硬件投粮成功后，若用户当前在 ESP32-S3 宠物时钟上选择展示该真实宠物（而非虚拟精灵），则播放"开心吃饭"动画 | 硬件：`main.cpp` 检查当前展示模式；后端：同步接口返回 `pet_animation: "eating"` | P1 |
| 真实宠物与虚拟精灵切换 | 用户可选择在硬件时钟上展示"真实宠物头像+健康数据"或"虚拟精灵动画" | 硬件/前端 | P2 |

#### 2.3 硬件方案说明

真实宠物硬件与现有 ESP32-S3 宠物时钟是**两套独立的物理设备**：

```
┌─────────────────────────────────────────────────────────────────┐
│                      DietAI 硬件生态                            │
│                                                                  │
│  ┌─────────────────────┐       ┌─────────────────────────────┐  │
│  │ ESP32-S3 宠物时钟   │       │ 智能喂食器 + 智能饮水机     │  │
│  │ （M1/M2 已实现）    │       │ （本方向新增，独立设备）    │  │
│  │                     │       │                             │  │
│  │ 服务对象：人        │       │ 服务对象：宠物              │  │
│  │ - 展示虚拟精灵动画   │       │ - 定时投粮（克数可控）     │  │
│  │ - 快捷按钮记录人    │       │ - 定时放水（水量可控）     │  │
│  │   的喝水/吃饭       │       │ - 投喂后 WiFi 上报数据     │  │
│  │ - 与人健康数据联动  │       │ - 余粮/缺水检测            │  │
│  └────────┬────────────┘       └──────────────┬──────────────┘  │
│           │                                    │                  │
│           ▼                                    ▼                  │
│    POST /api/hardware/sync          POST /api/hardware/sync       │
│    target_type=human                target_type=pet               │
│           │                                    │                  │
│           └──────────────┬─────────────────────┘                  │
│                          ▼                                        │
│              hardware_service.py                                  │
│              根据 target_type 走不同分支                           │
└─────────────────────────────────────────────────────────────────┘
```

**推荐方案：自研硬件**


   ESP32 主控（非 S3，基础款即可）
  WiFi + HTTP Client（复用宠物时钟联网代码）
   水泵驱动（饮水机定时放水）
   步进电机/舵机（控制出粮仓门开合角度 → 克数）
  称重传感器 HX711（食盆下方，前后称重差 = 实际出粮克数）
  定时任务（定时投喂 + 定时放水）
   ════════════════════════
 API：你的 /api/hardware/sync（已实现）
```

**喂食器核心原理：**

```
┌─────────────────────────────────────────────────────────┐
│                    投喂流程                              │
│                                                         │
│  ① 食盆称重传感器读取当前重量 W_before                   │
│       │                                                 │
│  ② ESP32 驱动步进电机旋转固定角度 → 粮仓门打开 → 猫粮落下│
│       │                                                 │
│  ③ 等待 2 秒（猫粮全部落入食盆）                         │
│       │                                                 │
│  ④ 食盆称重传感器读取当前重量 W_after                    │
│       │                                                 │
│  ⑤ actual_grams = W_after - W_before  ← 实际出粮克数    │
│       │                                                 │
│  ⑥ WiFi POST /api/hardware/sync                         │
│     {target_type: "pet", amount_grams: actual_grams,     │
│      food_name: "皇家猫粮", ...}                         │
│                                                         │
│  ⑦ ESP32 端累计消耗量 -= actual_grams                      │
│     若累计剩余 < 阈值 → 推送"粮仓余粮不足，请加粮"          │
└─────────────────────────────────────────────────────────┘
```

> 称重传感器不放在出粮口（猫粮下落时无法准确测量），而是放在食盆下方，通过投喂前后重量差计算实际出粮克数。**这样无论电机精度如何、猫粮颗粒大小是否均匀，都能拿到准确的投喂量。**
>
> **余粮检测不需要第二个传感器**：ESP32 端维护一个 `total_stock` 变量（用户加粮时手动设置初始值，如 500g），每次投喂后自动减去 `actual_grams`。当 `total_stock < 阈值`（如 50g）时推送提醒。纯软件逻辑，零硬件成本。

**硬件清单：**

| 类别 | 组件 | 用途 | 单价估计 |
|------|------|------|---------|
| 主控 | ESP32 开发板 | WiFi 联网 + GPIO 控制（非 S3，基础款够用） | ¥10-15 |
| 电机 | 28BYJ-48 步进电机 + ULN2003 驱动板 | 控制出粮仓门开合 | ¥8-12 |
| 水泵 | 微型水泵（5V） | 饮水机放水 | ¥5-10 |
| 水泵配件 | 硅胶管（内径 4-6mm） | 水仓 → 水碗导水 | ¥3-5 |
| 称重 | HX711 称重传感器模块 + 5kg 压力传感器 | 食盆下方，投喂前后称重差 = 实际克数 | ¥8-15 |
| 电源 | 5V/2A USB 电源适配器 + Micro-USB 线 | 给 ESP32、电机、水泵统一供电 | ¥10（实验室常有） |
| 粮仓 | 塑料密封罐（带盖，底部开孔） | 储存猫粮/狗粮，底部连接出粮口 | ¥5-10 |
| 水仓 | 塑料水壶/矿泉水瓶 | 储存饮用水，连接水泵进水管 | ¥0（空瓶利用） |
| 饭碗 | 普通宠物食盆 | 接住落下的猫粮 | ¥5-10 |
| 水碗 | 普通宠物水碗 | 接住水泵导出的水 | ¥5-10 |
| 外壳 | 3D 打印外壳 / 亚克力板 / 乐高 | 固定粮仓、电机、电路板 | 实验室自有 |
| **合计** | | | **¥60-100** |

> 之前的 ¥50 仅算了电子元件。加上饭碗、水碗、粮仓、硅胶管等结构件后约 ¥60-100。饭碗水碗家里有的话可以省掉。3D 打印外壳实验室有设备就不用花钱。

**开发节奏建议：**

```
第一周：搭电路（ESP32 + 步进电机 + 水泵 + 称重传感器），跑通"按键触发 → 电机转 → 猫粮落 → 读取称重差值"
第二周：接 WiFi，调通 POST /api/hardware/sync（上报实际称重克数），校准称重传感器
第三周：加定时任务 + 余粮检测 + 缺水检测
```

> 如果硬件进度不及预期，可先用 App 端「模拟投喂」按钮兜底——后端逻辑完全一致，答辩时至少能完整演示软件链路。

#### 2.4 验收标准

- [ ] 硬件投粮/放水后，后端正确写入 `pet_feeding_records` 和 `pet_water_records`
- [ ] 宠物每日营养汇总自动更新
- [ ] 硬件离线期间的数据缓存后，恢复网络时批量同步成功
- [ ] 手动录入和硬件自动记录两种方式共存，数据不冲突
- [ ] 不连接硬件时，App 端可手动记录宠物饮食

---

### 功能 3：宠物 AI 健康顾问（AI 增强层 / P1）

#### 3.1 需求背景

现有 AI Agent 体系已有 6 个 LangGraph 图，可新增一个轻量级的宠物健康顾问 Skill 或独立 Agent，基于宠物饮食历史、体重变化、品种特性给出建议。

#### 3.2 新增功能需求

##### 3.2.1 宠物营养分析

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 每日营养达标判断 | 根据宠物品种/年龄/体重/活动水平，计算每日推荐热量和蛋白质目标，判断当日摄入是否达标 | 后端：`pet_nutrition_calc.py`；AI：复用 `goal_tracking_agent` 思路 | P1 |
| 饮食趋势分析 | 分析近 7 天宠物饮食数据，识别营养缺口（如连续多日蛋白质偏低） | 后端/AI | P1 |
| 拍照录入宠物食品 | 用户拍摄宠物食品包装背面的**营养成分表** → OCR 提取文字 → 结构化解析（品牌、产品名、蛋白质、脂肪、热量等）→ 自动写入 `pet_food_database` 表 | AI：复用 DashScope OCR 能力；后端：新增 `parse_pet_food_label()` 解析函数 | P1 |
| 宠物食品库 | 用户录入的食品数据汇总，支持浏览、搜索、对比 | 前端：宠物食品库页面；后端：GET /api/pets/food-database | P1 |

##### 3.2.2 宠物健康建议

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 喂食量建议 | AI 根据宠物品种、体重、年龄、绝育状态推荐每日喂食量 | AI：`pet_health_agent` 或 `diet_deep_agent` 新增 Skill | P1 |
| 体重异常预警 | 当宠物体重连续 2 周异常变化（增长 >5% 或下降 >5%），AI 给出提示并建议就医 | 后端：定时任务；AI：预警文案生成 | P1 |
| 换粮建议 | 用户选择当前食品和新食品（均来自拍照录入的食品库），AI 对比两份真实营养成分，分析蛋白质/脂肪/热量差异，给出 7 天渐进过渡方案 | AI：`pet_health_agent` 新增 `compare_pet_foods` Skill | P1 |

##### 3.2.3 AI 对话扩展

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 宠物健康咨询对话 | 在 `chat_agent` 中增加 `session_type=6`（宠物健康咨询），用户可询问宠物相关健康问题 | AI：`chat_agent` prompts 扩展 | P2 |
| 合规免责声明 | 所有宠物健康建议附带免责："我是 AI 助手，建议仅供参考，宠物健康问题请咨询兽医" | AI：prompt 强制追加 | P0 |

#### 3.3 验收标准

- [ ] 宠物每日营养目标根据品种自动计算
- [ ] 饮食趋势分析能识别营养缺口
- [ ] 体重异常时给出预警提示
- [ ] 所有 AI 建议包含兽医咨询免责声明
- [ ] 没有硬件/AI 不可用时，基础功能仍可独立使用

---

### 功能 4：宠物健康数据可视化（体验层 / P1）

#### 4.1 需求背景

将宠物健康数据通过图表直观展示，提升用户体验。大量可复用现有前端图表组件（`fl_chart`）。

#### 4.2 新增功能需求

| 子功能 | 需求描述 | 涉及模块 | 优先级 |
|--------|---------|---------|--------|
| 宠物饮食日报 | 展示当日热量摄入进度环、蛋白质/脂肪/碳水分布饼图 | 前端：复用人类健康页的进度环和图表组件 | P1 |
| 宠物饮食周报 | 近 7 天热量摄入趋势折线图、与推荐值对比 | 前端：复用 `data_visualization_page` 的图表 | P1 |
| 体重变化曲线 | 体重变化 + 理想范围标注，支持 7/30/90 天切换 | 前端：复用 `weight_chart.dart` | P0 |
| 宠物健康评分 | 综合饮食达标率 + 体重管理 + 疫苗状态，给出 0-100 分 | 后端：计算逻辑；前端：评分卡片 | P2 |

#### 4.3 验收标准

- [ ] 宠物饮食日报正常展示
- [ ] 体重变化曲线准确反映历史数据
- [ ] 图表加载流畅，大数据量下不卡顿

---

### 功能 5：AI 自定义宠物形象（亮点层 / P0-P1）

#### 5.1 需求背景

目前虚拟桌宠的精灵形象使用预制的 GIF 素材，所有用户看到的都是同一套皮肤。真实宠物模块引入后，需要让用户为自家宠物生成**独一无二**的形象——每一只猫/狗的毛色、花纹、瞳色都不同，只有 AI 生成才能做到真正的个性化。

本功能采用**混合交付策略（方案 C）**：
- **P0（必做）**：AI 生成卡通形象 + 4 种情绪变体（静态 PNG）+ 前端伪动画，用户等待 ~35 秒即可使用
- **P1（可选升级）**：4 帧微变体静态图合成循环 GIF（如尾巴轻微摆动），后台静默生成

#### 5.2 用户侧体验流程

```
用户添加真实宠物 → 填写品种/毛色/瞳色等特征
                          │
           ┌──────────────┴──────────────┐
           ▼                              ▼
    「上传宠物照片」              「手动描述特征」
     (拍照或相册选择)             ("橘猫，白手套，绿眼睛")
           │                              │
           └──────────────┬──────────────┘
                          ▼
              点击「生成专属形象」
                          │
                          ▼
              ┌─────────────────────┐
              │  🎨 AI 正在生成...  │
              │  (约 15 秒)         │
              └─────────────────────┘
                          │
                          ▼
              ┌──────────────────────┐
              │  🐱 你的专属精灵     │
              │  [AI生成的卡通图]    │
              │                      │
              │  [满意，下一步]       │
              │  [重新生成]          │
              └──────────────────────┘
                          │ 用户确认 ✅
                          ▼
              ┌──────────────────────┐
              │  🔄 正在生成情绪动画 │
              │  ✅ 开心表情        │
              │  ✅ 正常表情        │
              │  🔄 饥饿表情...     │
              │  (约 20 秒)         │
              └──────────────────────┘
                          │
                          ▼
              ┌──────────────────────┐
              │  「你的精灵已就绪！」 │
              │                      │
              │  宠物详情页展示：     │
              │  - 4 种情绪静态图    │
              │  - 伪动画效果（呼吸  │
              │    眨眼/微摆/淡入淡出）│
              │  - "开心"GIF 后台生成 │
              └──────────────────────┘
```

#### 5.3 技术架构

```
┌──────────┐     ┌──────────────────┐     ┌────────────┐     ┌───────┐
│ Flutter   │────▶│  FastAPI 后端    │────▶│ 通义万相    │     │ MinIO │
│ 前端      │     │                  │     │ (DashScope) │     │       │
│           │     │ pet_avatar_      │     │             │     │       │
│ 拍照/描述 │     │ service.py       │     │ 图生图 API  │     │ 存储  │
│ 等待动画  │     │                  │     │             │     │       │
│ 展示结果  │◀────│ rembg 抠图       │◀────│             │────▶│ PNG   │
│           │     │                  │     │             │     │       │
│ 伪动画    │     │ (可选)豆包视频    │     │             │     │       │
│ 组件      │     │ → FFmpeg → GIF   │     │             │     │       │
└──────────┘     └──────────────────┘     └────────────┘     └───────┘
```

##### 5.3.1 步骤一：AI 图生图 — 卡通形象

调用通义万相（与现有 DashScope 同平台，接入成本极低），将用户照片或描述转换为 Q 版卡通形象：

```python
# 后端核心逻辑（伪代码）
prompt = f"""
将这张宠物照片转换为Q版卡通形象：
- 保留宠物的品种特征（{breed}）
- 保留毛色花纹位置（{pattern_description}）
- 保留瞳色（{eye_color}）
- 风格：扁平化矢量插画风，纯白色背景 #FFFFFF，正面全身
- 保持原宠物辨识度，让人一看就知道是同一只
"""

result = dashscope_image_generation.call(
    model="wan2.1-t2i-plus",
    prompt=prompt,
    reference_image=user_photo,       # 或纯文生图（无照片时）
    negative_prompt="写实风格, 照片风格, 恐怖, 畸形, 多只动物",
    size="1024x1024",
)
```

##### 5.3.2 步骤二：并行生成情绪变体

用确认后的卡通形象作为参考图，并行调用 4 次，每次生成不同情绪：

| 情绪 | Prompt 关键描述 |
|------|----------------|
| 开心 (happy) | 眯眼笑、耳朵竖起、尾巴上翘 |
| 正常 (normal) | 自然表情、安静站立 |
| 饥饿 (hungry) | 可怜巴巴、低头、耳朵下垂、看向食物 |
| 虚弱 (weak) | 趴下、眼睛半闭、精神不佳 |

> **一致性保证**：4 次调用使用同一张参考图 + 固定 seed（基于 pet_id 计算，如 `seed = pet_id * 10000`），确保生成的 4 张图看起来是同一只宠物。

##### 5.3.3 步骤三：rembg 自动抠图

使用开源抠图库 [rembg](https://github.com/danielgatis/rembg)（MIT 协议），无需自己编写抠图算法。

```python
# pip install rembg
from rembg import remove
from PIL import Image

for emotion in ["happy", "normal", "hungry", "weak"]:
    img = Image.open(f"pet_{emotion}.png")
    img_nobg = remove(img)  # AI 自动识别主体，去掉白色背景
    img_nobg.save(f"pet_{emotion}_nobg.png")
```

> **rembg 原理**：内置预训练 U-Net 深度学习模型（`isnet`），首次运行自动下载模型文件（~170MB）。本地 CPU/GPU 推理，完全免费，无需 API，无需联网。本项目仅需封装一层批量调用函数，核心抠图逻辑全部由 rembg 完成。

- `pip install rembg` 即用，零开发成本
- 卡通图（主体清晰、边缘分明）抠图准确率 >95%
- 4 张图总耗时约 10-20 秒（CPU）

##### 5.3.4 步骤四：前端伪动画

用代码让透明底静态 PNG 产生"会呼吸、会动"的感觉。**不需要 GIF / 视频 / Live2D**。

```dart
// 组件结构
Stack(
  children: [
    // 主图层：当前情绪图片 + 呼吸缩放 + 微旋转
    Transform.rotate(
      angle: sin(_controller.value * 2 * pi) * 0.03,  // ±1.7°
      child: ScaleTransition(
        scale: _breathingAnimation,  // 1.0 → 1.05 循环
        child: AnimatedCrossFade(     // 情绪切换时淡入淡出
          duration: Duration(milliseconds: 300),
          firstChild: Image.network(previousEmotionUrl),
          secondChild: Image.network(currentEmotionUrl),
          crossFadeState: _showNew ? CrossFadeState.showSecond : CrossFadeState.showFirst,
        ),
      ),
    ),
    // 眨眼遮罩层（定时显示/隐藏一张半透明闭眼PNG）
    if (_showBlink)
      Positioned.fill(
        child: Opacity(
          opacity: _blinkOpacity,
          child: Image.network(blinkOverlayUrl),
        ),
      ),
  ],
)
```

| 动画效果 | 实现方式 | 参数 |
|---------|---------|------|
| 呼吸缩放 | `ScaleTransition` + `AnimationController` 循环 | 1.0 → 1.05，周期 3s |
| 轻微摇摆 | `Transform.rotate` + sin 函数 | ±1.7°，周期 3s |
| 眨眼 | `Timer.periodic` 定时显示/隐藏闭眼遮罩 PNG | 每 4s 眨眼一次，持续 150ms |
| 情绪切换 | `AnimatedCrossFade` | 0.3s 交叉淡入淡出 |

**类比效果**：接近手机游戏中"角色待机动画"或 Duolingo 多邻国猫头鹰的动效——不是真正的逐帧动画，但看起来足够灵动。

##### 5.3.5 步骤五（P1 可选）：多帧微变体合成 GIF

不依赖视频生成模型（视频模型难以保证角色一致性，实际效果不可控），改用更可靠的方式——对同一角色多生成几张微变体关键帧，再用 FFmpeg 运动补偿插值补全中间帧。

```
同一张参考图 + 固定 seed → 通义万相图生图 × 4 次（关键帧）

  帧1: "同一只猫，尾巴微微向左摆"
  帧2: "同一只猫，尾巴摆到最左边"
  帧3: "同一只猫，尾巴摆回中间"
  帧4: "同一只猫，尾巴微微向右摆"

  4 张透明 PNG
       │
       ▼
  FFmpeg minterpolate 运动补偿插值（3 倍）
  4 帧 ← 插值 → 12 帧
       │
       ▼
  12 帧 @ 12fps = 1 秒循环 GIF（流畅度足够）
```

> **为什么是 4 关键帧而非 8-12 帧全 AI 生成**：多次图生图并行调用成本低（4 次），但如果生 12 帧——费用翻 3 倍且可能出现其中几帧角色不一致。4 关键帧 + FFmpeg 插值是成本和质量的最佳平衡点。
>
> **FFmpeg minterpolate**：`mci`（motion-compensated interpolation）模式分析相邻关键帧之间的像素运动，自动生成平滑过渡的中间帧。卡通风格边缘清晰、运动幅度小，插值几乎看不出痕迹。

| 对比 | 视频模型方案（已废弃） | 多帧静态图方案（采用） |
|------|---------------------|---------------------|
| 角色一致性 | ❌ 大概率变形 | ✅ 同 seed + 参考图，几乎不变 |
| 背景可控 | ❌ 绿幕不纯，抠像残留 | ✅ 纯白底 → rembg 一次搞定 |
| 成本 | ¥0.3/次 | ¥0.16（4 次图生图） |
| 生成耗时 | ~30s | ~20s（4 次并行） |
| 效果 | 不稳定，时好时坏 | 稳，类似手绘简笔动画 |

用户无需等待——P0 的静态图 + 伪动画立刻可用，GIF 后台静默生成后自动替换。

#### 5.4 降级与容错

| 场景 | 处理策略 |
|------|---------|
| AI API 不可用（欠费/网络） | 自动回退到预设品种分层素材（品种+颜色匹配），见 5.5 |
| AI 生成质量差（不像原宠物） | 前端提供「重新生成」按钮，每次调用随机微调 prompt 和 seed |
| 用户不想用 AI | 提供 6-8 种预设品种形象 + 颜色微调滑块（品红色相/饱和度） |
| 生成超时（>60s） | 前端显示超时提示，引导用户使用预设形象或稍后再试 |
| 绿幕抠像残留杂色 | 降级为 rembg 逐帧抠图处理 |

#### 5.5 预设品种兜底方案（不依赖 AI 时）

| 品种 | 基础毛色 | 可微调项 |
|------|---------|---------|
| 英短 | 蓝灰色 | 色相偏移（蓝灰→橘色→黑色） |
| 布偶 | 双色（白+棕） | 重点色深度 |
| 暹罗 | 重点色（米白+深棕） | 重点色区域&深度 |
| 橘猫 | 橘色虎斑 | 橘色饱和度、条纹密度 |
| 田园猫 | 灰/黑/白/三花 | 底色、花纹类型（纯色/虎斑/玳瑁） |
| 泰迪 | 棕色卷毛 | 棕色深浅、卷度 |
| 柯基 | 黄白双色 | 黄色深浅 |

> 预设方案不依赖任何 AI API，纯前端 `ColorFilter` + 分层 PNG 叠加实现。作为 AI 不可用时的绝对兜底，确保功能始终可用。

#### 5.6 后端接口设计

```
POST /api/pets/{pet_id}/generate-avatar

Request:
{
  "mode": "photo" | "description",
  "photo": "<base64>",                          // mode=photo 时
  "description": "橘猫，白手套，绿眼睛"          // mode=description 时
}

Response (202 Accepted):
{
  "task_id": "gen_abc123",
  "status": "processing",
  "estimated_seconds": 15
}
```

```
GET /api/pets/generation-tasks/{task_id}

Response (processing):
{
  "task_id": "gen_abc123",
  "status": "processing",
  "progress": "generating_base_image"  // generating_base_image | generating_emotions | done
}

Response (done):
{
  "task_id": "gen_abc123",
  "status": "done",
  "result": {
    "base_image_url": "https://minio.xxx/pet_avatars/abc123_base.png",
    "emotions": {
      "happy": "https://minio.xxx/pet_avatars/abc123_happy.png",
      "normal": "https://minio.xxx/pet_avatars/abc123_normal.png",
      "hungry": "https://minio.xxx/pet_avatars/abc123_hungry.png",
      "weak": "https://minio.xxx/pet_avatars/abc123_weak.png"
    },
    "seed": 123456789,
    "has_gif": false    // P0 阶段为 false，P1 升级后为 true
  }
}
```

```
POST /api/pets/{pet_id}/regenerate-emotion  （可选）
POST /api/pets/{pet_id}/upgrade-to-gif      （P1 可选，触发 GIF 生成）
```

#### 5.7 验收标准

- [ ] 用户可通过拍照或文字描述生成专属卡通宠物形象
- [ ] 4 种情绪变体（开心/正常/饥饿/虚弱）并行生成，总耗时 < 40 秒
- [ ] 生成结果自动抠图，透明背景 PNG 正确显示
- [ ] 前端伪动画：呼吸缩放、眨眼、微旋转、情绪切换淡入淡出均正常
- [ ] AI 不可用时自动降级为预设品种形象
- [ ] 不满意时可重新生成，每次结果不同
- [ ] （P1）"开心"GIF 真动画后台生成并自动替换，透明背景无残留
- [ ] 宠物详情页正确展示自定义形象（替代默认品种图标）

#### 5.8 费用预估

| 步骤 | API | 调用次数 | 单价 | 小计 |
|------|-----|---------|------|------|
| 图生图（基础形象） | 通义万相 | 1 次 | ¥0.04 | ¥0.04 |
| 图生图（情绪变体 ×4） | 通义万相 | 4 次 | ¥0.04 | ¥0.16 |
| 抠图 | rembg（本地）| 5 张 | 免费 | ¥0 |
| **P0 合计** | | | | **~¥0.20/只宠物** |
| 微变体图生图 × 4（合成 GIF） | 通义万相 | 4 次 | ¥0.04 | ¥0.16 |
| 合成 GIF | FFmpeg（本地）| 1 次 | 免费 | ¥0 |
| **P0+P1 合计** | | | | **~¥0.36/只宠物** |

> 学生认证或比赛场景可能申请到通义万相/即梦的免费额度，实际费用可能为 0。

---

## 三、数据库新增需求

### 3.1 新增表

| 表名 | 核心字段 | 说明 |
|------|---------|------|
| pet_profiles | id, user_id(FK), name(varchar), species(varchar:cat/dog/other), breed(varchar), gender(varchar), birth_date(date), is_neutered(bool), avatar_url(varchar), is_active(bool), created_at, updated_at | 真实宠物档案 |
| pet_weight_records | id, pet_id(FK), weight(decimal), measured_at(datetime), notes(text), created_at | 宠物体重记录 |
| pet_vaccine_records | id, pet_id(FK), vaccine_name(varchar), vaccinated_at(date), expiry_date(date), next_vaccination_date(date), notes(text), created_at | 疫苗记录 |
| pet_deworming_records | id, pet_id(FK), deworming_type(varchar:internal/external), treated_at(date), next_treatment_date(date), notes(text), created_at | 驱虫记录 |
| pet_feeding_records | id, pet_id(FK), food_name(varchar), amount_grams(decimal), calories(decimal), protein(decimal), fat(decimal), carbs(decimal), record_time(datetime), from_source(varchar:hardware/manual), created_at | 宠物饮食记录 |
| pet_water_records | id, pet_id(FK), amount_ml(int), record_time(datetime), from_source(varchar:hardware/manual), created_at | 宠物饮水记录 |
| pet_daily_summaries | id, pet_id(FK), summary_date(date), total_calories(decimal), total_protein(decimal), total_fat(decimal), total_carbs(decimal), total_water_ml(int), meal_count(int), created_at, updated_at | 宠物每日营养汇总 |
| pet_food_database | id, food_name(varchar), brand(varchar), category(varchar:dry_food/wet_food/snack), suitable_species(varchar:cat/dog), calories_per_100g(decimal), protein_per_100g(decimal), fat_per_100g(decimal), carbs_per_100g(decimal), created_at | 宠物食品营养库 |
| pet_avatars | id, pet_id(FK, unique), base_image_url(varchar), emotion_happy_url(varchar), emotion_normal_url(varchar), emotion_hungry_url(varchar), emotion_weak_url(varchar), generation_seed(int), has_gif(bool, default=false), gif_url(varchar, nullable), prompt_used(text), ai_model(varchar), created_at, updated_at | AI 生成宠物形象（一宠一记录） |

### 3.2 修改现有表

| 表名 | 修改内容 | 说明 |
|------|---------|------|
| hardware_quick_buttons | 新增 pet_id(FK, nullable)，当 target_type=pet 时关联宠物 | 硬件快捷按钮支持宠物模式 |
| offline_sync_log | 新增 target_type(varchar, default='human') | 记录同步目标类型 |

> 注：`HardwareQuickButton` 和 `OfflineSyncLog` 已在 `shared/models/pet_models.py` 中定义（当前用于虚拟宠物），需要检查是否需要拆分为独立文件。

### 3.3 数据库索引建议

| 索引名 | 表 | 字段 |
|--------|-----|------|
| idx_pet_feeding_pet_time | pet_feeding_records | (pet_id, record_time DESC) |
| idx_pet_water_pet_time | pet_water_records | (pet_id, record_time DESC) |
| idx_pet_daily_summary_date | pet_daily_summaries | (pet_id, summary_date) |
| idx_pet_weight_pet_time | pet_weight_records | (pet_id, measured_at DESC) |
| idx_pet_vaccine_next_date | pet_vaccine_records | (next_vaccination_date) WHERE next_vaccination_date IS NOT NULL |
| idx_pet_food_db_species | pet_food_database | (suitable_species, category) |

---

## 四、API 新增需求

### 4.1 宠物档案

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/pets | 创建宠物档案 |
| GET | /api/pets | 获取当前用户的宠物列表 |
| GET | /api/pets/{id} | 获取宠物详情 |
| PUT | /api/pets/{id} | 更新宠物信息 |
| DELETE | /api/pets/{id} | 删除宠物（软删除） |

### 4.2 宠物体重

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/pets/{id}/weight-records | 记录体重 |
| GET | /api/pets/{id}/weight-records | 查询体重记录 |
| GET | /api/pets/{id}/weight-trend | 体重趋势（7/30/90天） |
| DELETE | /api/pets/{id}/weight-records/{record_id} | 删除体重记录 |

### 4.3 宠物饮食（硬件联动 + 手动）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/pets/{id}/feeding-records | 手动记录宠物饮食 |
| GET | /api/pets/{id}/feeding-records | 查询宠物饮食记录 |
| GET | /api/pets/{id}/daily-summary/{date} | 宠物每日营养汇总 |
| GET | /api/pets/{id}/feeding-plan | 获取推荐喂食计划 |

### 4.4 宠物疫苗/驱虫

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/pets/{id}/vaccine-records | 添加疫苗记录 |
| GET | /api/pets/{id}/vaccine-records | 查询疫苗记录 |
| POST | /api/pets/{id}/deworming-records | 添加驱虫记录 |
| GET | /api/pets/{id}/deworming-records | 查询驱虫记录 |

### 4.5 硬件联动（修改现有）

| 方法 | 路径 | 修改内容 |
|------|------|---------|
| POST | /api/hardware/sync | 请求体增加 target_type(human/pet)，pet_id 字段；后端根据 target_type 走不同分支 |
| GET | /api/hardware/quick-buttons/{user_id} | 支持查询宠物专用快捷按钮（喂食/加水/零食） |

### 4.6 宠物食品库

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/pets/food-database | 查询宠物食品库（支持按品种、分类筛选） |
| GET | /api/pets/food-database/search | 搜索宠物食品（关键词匹配） |

### 4.7 宠物 AI 咨询（可选）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/pets/{id}/ai-advice | 获取宠物 AI 健康建议（非流式） |

### 4.8 AI 自定义宠物形象

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/pets/{pet_id}/generate-avatar | 触发 AI 生成宠物形象（异步，返回 task_id） |
| GET | /api/pets/generation-tasks/{task_id} | 查询生成任务状态与结果 |
| POST | /api/pets/{pet_id}/regenerate-emotion | 重新生成单个情绪变体 |
| POST | /api/pets/{pet_id}/upgrade-to-gif | 触发"开心"情绪 GIF 真动画生成（P1） |

---

## 五、前端新增/修改需求

### 5.1 新增页面

| 页面 | 文件名 | 说明 |
|------|--------|------|
| 我的宠物（改版） | `my_pet_page.dart`（改造） | 新增"真实宠物"Tab，展示宠物列表+健康概览 |
| 添加宠物 | `add_pet_page.dart` | 品种选择、基本信息录入 |
| 宠物详情页 | `pet_detail_page.dart`（改造/新增） | 体重趋势图、饮食日报、疫苗/驱虫时间线 |
| 宠物饮食记录 | `pet_feeding_page.dart` | 手动记录宠物饮食、查看历史 |
| 宠物食品库 | `pet_food_library_page.dart` | 浏览/搜索宠物食品营养数据 |
| AI 生成宠物形象 | `generate_pet_avatar_page.dart` | 拍照/描述 → AI 生成 → 预览 → 确认 |
| 宠物形象生成进度 | `avatar_generation_progress.dart` | 展示生成进度（基础图 → 情绪变体 → 抠图 → 完成） |

### 5.2 修改现有页面

| 页面 | 修改内容 |
|------|---------|
| `my_pet_page.dart` | 增加顶部 Tab（虚拟精灵 / 真实宠物），真实宠物 Tab 展示宠物卡片列表、健康概览 |
| `home_page.dart` | 如有真实宠物，首页可切换展示"我的健康"或"XX(宠物名)的健康" |
| `hardware` 相关 | 快捷按钮配置支持宠物模式 |

### 5.3 新增/复用组件

| 组件 | 说明 | 复用来源 |
|------|------|---------|
| 宠物品种选择器 | 品种搜索+选择 | 新建 |
| 宠物饮食进度环 | 宠物当日热量摄入进度 | 复用 `water_intake_widget.dart` 的进度环样式 |
| 体重趋势图 | 体重变化+理想范围 | 复用 `weight_chart.dart` |
| 疫苗时间线 | 疫苗/驱虫到期倒计时 | 新建，参考提醒列表样式 |
| 宠物健康评分卡 | 综合评分 | 参考人类健康评分卡片 |
| AI 宠物形象展示组件 | 透明底 PNG + 伪动画（呼吸/眨眼/摇摆/淡入淡出）+ 情绪切换 | 新建，可替代默认品种图标 |
| 宠物形象降级展示 | AI 不可用时使用预设品种分层素材（`ColorFilter` + 分层 PNG 叠加） | 新建，品种列表见 5.5 |

---

## 六、AI Agent 新增需求

### 6.1 新增 Skill

| 组件 | 说明 | 优先级 |
|------|------|--------|
| pet_nutrition_skill | 根据宠物品种/年龄/体重计算每日营养目标 | P1 |
| pet_health_advice_skill | 基于宠物饮食历史+体重趋势+品种特性生成健康建议 | P1 |
| pet_weight_alert_skill | 检测体重异常变化，生成预警文案 | P1 |

### 6.2 修改现有 Agent

| Agent | 修改内容 | 优先级 |
|-------|---------|--------|
| `diet_deep_agent` | 新增 pet_health 相关 Skills；新增工具：`get_pet_profile`、`get_pet_daily_summary`、`get_pet_weight_trend` | P1 |
| `chat_agent` | 新增 `session_type=6`（宠物健康咨询），增加宠物健康系统提示词 | P2 |

### 6.3 Prompt 要求

- 所有宠物健康建议必须包含："我是 AI 助手，建议仅供参考，宠物健康问题请咨询专业兽医"
- 不提供药物推荐、不提供诊断结论
- 遇到"呕吐、腹泻、不吃不喝超过24小时"等关键词，强制建议立即就医

---

## 七、与现有系统的复用分析

### 7.1 可直接复用的模块

| 现有模块 | 复用方式 | 复用率 |
|---------|---------|--------|
| `hardware_router.py` / `hardware_service.py` | 新增 `target_type=pet` 分支，核心同步逻辑完全复用 | ~80% |
| `water_models.py` → `WaterIntakeRecord` | `pet_water_records` 表结构与 `water_intake_records` 高度相似 | ~70% |
| `food_models.py` → 营养字段定义 | `pet_feeding_records` 参考 `NutritionDetail` 的字段设计 | ~60% |
| `daily_nutrition_summaries` 汇总逻辑 | `pet_daily_summaries` 的 upsert 逻辑复用 | ~70% |
| `weight_chart.dart`（前端体重图表） | 直接复用，数据源从 `weight_records` 切换到 `pet_weight_records` | ~80% |
| `reminder` 模块（提醒设置+定时检查） | 疫苗/驱虫提醒直接复用提醒框架 | ~90% |
| `fl_chart` 图表组件 | 复用进度环、折线图、饼图 | ~90% |
| `notification_service.dart` | 本地通知复用 | ~100% |
| AI Agent LangGraph 框架 | 新增 Skill，框架零改动 | ~100% |
| `nutrition_calc.py` | 新增宠物营养目标计算函数，参考人类 BMR/TDEE 逻辑 | ~50% |

### 7.2 需要从零新建的模块

| 模块 | 说明 |
|------|------|
| `pet_profiles` 数据模型 + CRUD | 全新，与现有宠物模型（虚拟精灵）概念不同 |
| 宠物品种数据（猫/狗品种列表） | 需要整理数据（JSON 配置文件即可） |
| 宠物食品营养库 | 需要收集常见猫粮/狗粮/零食的营养数据 |
| 前端宠物档案相关页面 | 全新 UI，但样式可参考人类健康页 |

---

## 八、工作量评估

### 8.1 按功能分层

| 层级 | 功能 | 后端工作量 | 前端工作量 | AI工作量 |
|------|------|-----------|-----------|---------|
| P0 基础 | 宠物档案 CRUD + 体重/疫苗记录 | 3-4 天 | 3-4 天 | — |
| P0 核心 | 硬件联动自动饮食记录 | 2-3 天 | 1-2 天 | — |
| P0 亮点 | AI 自定义宠物形象（静态图+伪动画） | 2 天 | 2 天 | — |
| P1 增强 | 营养分析 + 健康建议 | 2 天 | 2 天 | 2-3 天 |
| P1 体验 | 数据可视化 + 饮食周报 | 1-2 天 | 2-3 天 | — |
| P1 可选 | 多帧微变体合成 GIF（图生图 × 4 → FFmpeg） | 1 天 | 0.5 天 | — |
| P2 展望 | AI 对话 + 宠物食品库 | 1-2 天 | 1-2 天 | 1-2 天 |

### 8.2 汇总

| 范围 | 后端 | 前端 | AI | 合计 |
|------|------|------|----|------|
| 仅 P0（最小可展示） | ~8 天 | ~7 天 | 0 | ~15 人天 |
| P0 + P1（完整体验） | ~13 天 | ~12 天 | ~3 天 | ~28 人天 |
| 全部（含 P2） | ~15 天 | ~14 天 | ~5 天 | ~34 人天 |

> **对比参考**：M2 四大功能（消费统计 + 虚拟宠物 + AI 风格 + 轻断食）总工作量约 30 人天（10 天 × 3 人）。本方向 P0+P1 约 28 人天，且大量复用现有代码，实际可能更低。

### 8.3 开发周期

**总周期：10 天**（与 M2 对齐）。

> **与 M2 的关键变化**：M2 采用串行（后端完成 → 前端拉代码 → 开发 → 硬件整合）。M3 不同——硬件需要新造物理设备，且 API 协议已在本文档明确定义。**本次改并行：三人同时开工，第 5-6 天合流。**
>
> **硬件材料的特殊性**：网购到货需 3 天。前 3 天硬件同学先在电脑上写 ESP32 固件代码（Arduino 可脱离硬件编译验证），同时协助协调后端/前端的对接节奏。材料到货后立刻组装，代码已就绪，只需烧录+接线+验证。

```
               Day 1-2    Day 3-4    Day 5-6    Day 7-8    Day 9-10
               ───────    ───────    ───────    ───────    ────────
后端（何丽）    DB建表+    饮食记录    AI图生图+   拍照OCR+    联调+
              档案CRUD   API         rembg      换粮建议    兜底

前端/Agent    UI骨架     对接档案    对接饮食   伪动画组件+  联调+
（张正宏）     (空壳+      API+       图表API    情绪展示    兜底
              假数据)    品种选择器  

统筹/硬件     【网购等3天】写固件代码    到货！组装    联网对接    全链路
（胡馨）       采购下单   （Arduino     电路+       API         联调+
                        编译验证）    串口调试    硬件demo    录制
```

**各阶段交付物：**

| 天数 | 后端（何丽） | 前端/Agent（张正宏） | 统筹/硬件（胡馨） |
|------|------|-----------|------|
| 1-2 | pet_profiles / pet_weight_records / pet_vaccine_records 表 + CRUD API | 宠物列表页骨架（Tab 切换 + 卡片占位）、品种选择器 UI | 下单采购全部材料；在 Arduino IDE 中编写电机驱动/水泵控制/称重传感器读取代码，编译通过（脱离硬件验证语法） |
| 3-4 | /api/hardware/sync pet 分支、pet_feeding_records / pet_water_records API | 对接档案 API、宠物详情页数据展示 | 材料到货（第 3 天），组装电路 + 烧录已写好的代码 → 串口验证"电机转→水泵抽→称重读数" |
| 5-6 | AI 图生图 + rembg 抠图 pipeline，宠物每日营养汇总 | 对接饮食记录 API、饮食日报 + 体重趋势图 | 串口验证通过，编写 WiFi + HTTP 联网代码 |
| 7-8 | 拍照 OCR 录入食品 + 换粮对比 API | 伪动画组件（呼吸/眨眼/摇摆）、4 情绪切换展示 | 对接后端 /api/hardware/sync，验证投喂数据上报全流程 |
| 9-10 | 全链路联调、预设品种降级兜底 | 全链路联调、UI 打磨 | 全链路联调（投喂→API→前端刷新→精灵情绪联动）、demo 录制 |

> **硬件 3 天空窗期不闲置**：Arduino 代码可以脱离硬件编写和编译验证。电机控制、传感器读取、HTTP POST 这些代码完全可以在到货前写完。到货后只需烧录+接线，1 天内完成物理验证。
>
> **前端无需等待后端完成**：API 协议已在文档中明确定义，前端可先用硬编码假数据搭建 UI 骨架，后端 API 出一个接一个。
>
> M2 实际用时不到一周完成 10 天规划，本次 10 天预留充足缓冲。第 10 天为风险缓冲。

### 8.4 数据库新增表

- P0：7 张表（pet_profiles, pet_weight_records, pet_vaccine_records, pet_feeding_records, pet_water_records, pet_daily_summaries, pet_avatars）
- P1：2 张表（pet_deworming_records, pet_food_database）
- P2：—

**总计：8-9 张新表**（对比 M2 新增 5 张表 + 修改 3 张表）

### 8.5 API 新增

- P0：约 18 个端点
- P1：约 5 个端点
- P2：约 3 个端点

**总计：约 26 个新 API 端点**（对比 M2 新增 16 个 + 修改 5 个）

---

## 九、风险与注意事项

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| 项目定位变散 | 从"人的健康"变成"人+宠物"，核心叙事可能模糊 | 将宠物功能定位为"DietAI 生态延伸"而非主体；答辩时强调"同一套 AI + 硬件技术栈服务两个场景" |
| 硬件喂食器接入不可控 | 如果没有真实硬件，自动记录功能无法演示 | 最低成本方案：在 ESP32-S3 上增加 2 个按键模拟喂食/加水，按下即触发 API，可完整演示整条链路 |
| 宠物营养数据不准确 | AI 建议可能出错 | 宠物食品库数据来源标注清楚（品牌官方数据优先）；AI 建议强制包含兽医免责 |
| 现有虚拟宠物功能混淆 | 用户不理解"虚拟精灵"和"真实宠物"的区别 | UI 设计上明确拆分 Tab，命名清晰：「精灵伙伴」vs「我的宠物」 |
| 前端改动涉及 my_pet_page.dart 大改 | 该页面 ~1500 行，改动风险高 | 不直接大改现有页面，新增独立页面 + Tab 切换，现有代码保持不变 |
| 数据库表膨胀 | 新增 8-9 张表，维护成本增加 | 优先只建 P0 的 7 张表，P1/P2 的表按需再建 |
| AI 图像生成质量不稳定 | 生成的宠物形象可能不像、情绪变体一致性差 | 预设品种兜底方案；seed 固定 + 参考图约束保证一致性；答辩前提前录好演示视频 |

---

## 十、演示/答辩建议

### 10.1 最小可行演示路径（仅 P0）

```
1. 用户打开 App → "我的宠物" → 添加一只猫（品种：英短，2岁，已绝育）
2. 点击「生成专属形象」→ 上传猫的照片 → AI 生成卡通形象（15秒）
3. 确认后生成 4 种情绪变体（20秒）→ 宠物档案展示带呼吸眨眼的精灵形象
4. 展示宠物详情页：体重趋势图、疫苗记录时间线
5. （硬件演示）按下 ESP32-S3 上的「喂食」按钮 →
   App 刷新 → 宠物详情页显示"今日已喂食：皇家猫粮 50g，210kcal"
6. （硬件演示）按下「加水」按钮 →
   App 刷新 → 显示"今日饮水：150ml"
7. 展示宠物每日营养汇总："今日热量 210/250 kcal（达标），蛋白质 17.5/20g"
8. 精灵情绪联动：喂食后精灵切换为"开心"表情，缺水时切换为"饥饿"表情
```

### 10.2 答辩核心叙事

> "DietAI 最初关注人的饮食健康，但我们发现很多用户同时也是养宠人士。
> 我们在 M2 的虚拟宠物基础上，探索了真实宠物健康管理方向。
> 
> 这个方向有三大亮点：
> 
> **第一，AI 生成专属宠物形象。** 用户只需拍一张照片，系统在 35 秒内生成独一无二的
> Q 版宠物精灵形象，包含开心、正常、饥饿、虚弱 4 种情绪变体。
> 我们通过固定 seed 和参考图约束，保证了多次生成的角色一致性。
> 
> **第二，硬件联动——智能喂食器定时投粮时自动生成饮食记录，** 
> 用户完全不需要手动录入。
> 
> **第三，情绪智能联动。** 精灵会根据硬件投喂状态自动切换情绪——
> 刚喂完食会开心，缺水时表现出饥饿，体重超标时显示虚弱。
> 
> 技术层面，我们复用了 DietAI 现有的 AI 营养分析引擎、硬件对接协议、
> 数据可视化组件，用不到 30% 的新增代码实现了完整的宠物健康管理链路。
> 这让 DietAI 从「管好自己的健康」升级为「管好全家的健康」。"


---

## 十一、附录

### 附录 A：相关文档

- [DietAI Milestone 1 需求文档](../milestone1/Milestone1_需求文档.md)
- [DietAI Milestone 2 PRD](../milestone2/DietAI_Milestone2_PRD.md)
- [智能宠物时钟硬件规划](../hardware/智能宠物时钟硬件规划.md)
- [DietAI 架构设计文档](../../DietAI/docs/需求文档/架构设计文档.md)

### 附录 B：本方向与 M2 虚拟宠物功能的关系

| M2 功能 | 本方向处理方式 |
|---------|--------------|
| `virtual_pet_states` 表 | 保留不变，服务于虚拟精灵养成（mood/level/exp/skin） |
| `pet_unlockables` 表 | 保留不变 |
| `my_pet_page.dart` | 新增「真实宠物」Tab，不删除现有虚拟精灵功能 |
| 硬件端宠物状态同步 | 保留不变，虚拟精灵动画和真实宠物数据可共存在同一硬件上 |
| `pet_router.py` | 保留不变（服务于虚拟宠物）；新建 `real_pet_router.py`（服务于真实宠物），两套路由独立 |

### 附录 C：宠物品种数据示例（JSON 配置）

```json
{
  "species": "cat",
  "breeds": [
    {"name": "英国短毛猫", "avg_weight_kg": {"male": 5.5, "female": 4.5}, "daily_calories_per_kg": 50},
    {"name": "布偶猫", "avg_weight_kg": {"male": 7.0, "female": 5.5}, "daily_calories_per_kg": 45},
    {"name": "暹罗猫", "avg_weight_kg": {"male": 4.5, "female": 3.5}, "daily_calories_per_kg": 55},
    {"name": "中华田园猫", "avg_weight_kg": {"male": 5.0, "female": 4.0}, "daily_calories_per_kg": 50}
  ]
}
```

### 附录 D：宠物食品营养库数据示例（JSON 配置）

```json
{
  "brand": "皇家",
  "foods": [
    {"name": "皇家室内成猫粮", "species": "cat", "category": "dry_food",
     "calories_per_100g": 380, "protein_per_100g": 34.0,
     "fat_per_100g": 14.0, "carbs_per_100g": 35.0},
    {"name": "皇家幼猫粮", "species": "cat", "category": "dry_food",
     "calories_per_100g": 410, "protein_per_100g": 36.0,
     "fat_per_100g": 18.0, "carbs_per_100g": 30.0}
  ]
}
```

---

*文档生成日期：2026-07-16*

